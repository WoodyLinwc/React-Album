on: push

jobs: 
  npm-build: 
    - name: Environment
      runs-on: ubuntu-latest

    steps:
    - name: Read Repo Content
      uses: actions/checkout@v4

    - name: Install Dependencies
      run: npm install

    - name: Wrap the Project
      run: npm run build